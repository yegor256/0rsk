%p
  Talk to our Telegram bot:
  %a{href: 'https://t.me/zerorsk_bot'}<
    %code @zerorsk_bot

%form{method: 'GET', action: ''}
  %input{type: 'text', name: 'q', size: 40, autocomplete: 'off', autofocus: 'true', tabindex: 1, value: query}
  %input{type: 'submit', value: 'Search', tabindex: 2}

- if tasks.empty?
  %p
    The agenda is empty.
- else
  - tasks.each do |t|
    %p
      %code= "T#{t[:id]}"
      = '&rarr;'
      %code= "P#{t[:plan]}"
      = '&rarr;'
      %code= "#{t[:part]}"
      %br
      &= t[:ptext]
      %br
      &= t[:text]
      %br
      %a.item.small{href: iri.cut('/project').add(id: t[:pid])}<
        &= t[:title]
      %span.item.small<
        &= t[:schedule]
      %a.item.small{href: iri.cut('/tasks/done').add(id: t[:id], plan: t[:plan], part: t[:part])} Done
  %p
    - if offset.positive?
      %a.item{href: iri.over(offset: offset - 25)} Previous
    - if tasks.count >= limit
      %a.item{href: iri.over(offset: offset + 25)} Next

